{
  "name": "vending-identity-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "人脸识别参考服务（1:N 识别 + 活体 + 特征向量登记，INV-8）—— commerce-harness identity 适配器的可部署端点",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types test/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
